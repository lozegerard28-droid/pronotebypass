name: PronoteBypass
options:
  bundleIdPrefix: fr.pronotebypass
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
targets:
  PronoteBypass:
    type: application
    platform: iOS
    sources:
      - path: PronoteBypass
    settings:
      base:
        INFOPLIST_FILE: PronoteBypass/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: fr.pronotebypass.app
